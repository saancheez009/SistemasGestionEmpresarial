@using Ejercicio1.Models.Entities;
@model List<Ejercicio1.Models.Entities.clsPersona>
@{
    ViewData["Title"] = "ListadoPersonas";
    var listadoPersonas = ViewBag.listPers;
}
<style >
    table {
        border-collapse: collapse;
    }

    td {
        padding: 5px;
        text-align: left;
    }
</style>

<h1>ListadoPersonas</h1>
<table >
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Apellido</th>
        </tr>
    </thead>
    @* @{
        for(int i =0; i < Model.Count; i++)
        {
            <tr>
                <td>@Model[i].Nombre</td>
                <td>@Model[i].Apellido</td>
            </tr>
        }
    } *@

    @{
        foreach (clsPersona pers in Model)
        {
            <tr>
                <td>@pers.Nombre</td>
                <td>@pers.Apellido</td>
            </tr>

        }
        
    }
</table>

@Html.ActionLink("Volver al Index", "Index")
